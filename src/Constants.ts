'use strict';

export namespace Constants {
    export enum Action {
        Compile,
        Run,
        CompileRun,
        CustomCompile,
        CustomRun,
        CustomCompileRun,
        ExternalCompileRun,
        ExternalCustomCompileRun
    }
}
